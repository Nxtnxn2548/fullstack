const furniture = ['Table', 'Chairs', 'Couch'];

for (let i = 0; i < furniture.length; i++) {
    console.log(furniture[i]);

    const word = furniture[i];
    for (let j = 0; j < word.length; j++) {
        console.log(word[j]);    }
}
